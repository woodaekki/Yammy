stages:
  - Test_Build
  - FE_Image
  - BE_Image
  - Deploy

variables:
  ECR_REPOSITORY_URI: 725115073641.dkr.ecr.ap-northeast-2.amazonaws.com/yammy
  APP_DIR: .
  FRONTEND_DIR: Yammy-FE
  BACKEND_DIR: Yammy-BE/yammy

# =========================================================
# 🎯 Test_Build_BE: 테스트 환경 구성 및 빌드 (확인 대상)
# =========================================================
Test_Build_BE:
  stage: Test_Build
  image: openjdk:17-jdk-slim
  tags:
    - test-build # docker executore를 사용하는 새로 생성된 러너
  services:
    - name: mysql:8.0
      alias: mysql-db
    - name: redis:latest
      alias: redis

  variables:
    # 1. MySQL 서비스 컨테이너 자체 설정 변수
    MYSQL_DATABASE: testdb
    MYSQL_ROOT_PASSWORD: root_password

    # 2. Spring Boot가 참조할 환경 변수 (GitLab CI에서 조합)
    DB_URL: jdbc:mysql://mysql-db:3306/${MYSQL_DATABASE}?serverTimezone=Asia/Seoul
    DB_USERNAME: root
    DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}

    SPRING_DATA_REDIS_HOST: redis
    SPRING_DATA_REDIS_PORT: 6379
    SPRING_DATA_REDIS_PASSWORD:

    # 추가 변수들은 GitLab CI/CD Variables에 등록되어 사용된다고 가정

  before_script:
    - cd ${BACKEND_DIR}
    - chmod +x gradlew

    # ⭐️ 추가됨: 기존 application.yml 파일 삭제 ⭐️
    - echo "Deleting existing application.yml and creating CI version..."
    - rm -f src/main/resources/application.yml

    # 핵심: CI용 YML 내용을 디스크에 application.yml로 생성
    - echo "$CI_APPLICATION_YML" > src/main/resources/application.yml
    - echo "application.yml created successfully with CI variables."

  script:
    - ./gradlew clean build

  artifacts:
    when: on_success
    paths:
      - ${BACKEND_DIR}/build/libs/*.jar
    expire_in: 1 week

# =========================================================
# ❌ 나머지 스테이지: 실행 방지 (테스트 확인용)
# =========================================================

FE_Image:
  stage: FE_Image
  image: node:20-alpine # Node 환경에서 빌드 (Alpine이 가볍습니다)
  tags:
      - yammy-build
  variables:
    # ⭐️ ECR 로그인 및 AWS CLI에 사용될 변수 (GitLab CI/CD Variables에 등록) ⭐️
    AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
    AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
    AWS_DEFAULT_REGION: ap-northeast-2

    # Dockerfile 및 최종 이미지 경로 설정
    FE_IMAGE_URI: $ECR_REPOSITORY_URI/yammy:latest

  before_script:
    - cd ${FRONTEND_DIR}

    # 1. NPM 패키지 설치 및 빌드
    - npm install
    - npm run build

    # 2. AWS CLI 설치 및 ECR 로그인 (Docker 명령을 위해 ECR 인증)
    # Runner 환경에 AWS CLI가 없으면 이 과정을 거쳐야 합니다.
    - apk add --no-cache python3 py3-pip # Alpine 리눅스 기반 이미지에서 AWS CLI 설치
    - pip install awscli
    - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $ECR_REPOSITORY_URI

  script:
    # 3. Docker 이미지 빌드
    # Dockerfile은 FE_Image/Yammy_FE 디렉토리에 있다고 가정
    - docker build -t $FE_IMAGE_URI .

    # 4. ECR에 푸시
    - docker push $FE_IMAGE_URI

BE_Image:
  stage: BE_Image
  script:
    - echo "Skipping BE Image build for Test."
  when: manual
  allow_failure: true

Deploy:
  stage: Deploy
  script:
    - echo "Skipping Deploy for Test."
  when: manual
  allow_failure: true